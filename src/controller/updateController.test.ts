import request from 'supertest'
import app from '../index'

describe('UpdateController', () => {
    it('/$/update', () => {

    })

    it('/$/updateProfile', () => {

    })

    it('/$/updateBoard/:id', () => {

    })

    it('/$/updateSkill', () => {

    })

    it('/$/updateTarget', () => {
        
    })
})