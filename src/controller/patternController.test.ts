import request from 'supertest'
import app from '../index'

describe('PatternController', () => {
    it('/rank/:gtype/:page', () => {

    })

    it('/pattern/:ver/:order/:page', () => {

    })

    it('/ptrank/:mid/:ptcode/:page/:version', () => {

    })

    it('/cntrank/:page', () => {
        
    })
})