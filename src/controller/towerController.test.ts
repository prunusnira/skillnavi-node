import request from 'supertest'
import app from '../index'

describe('TowerController', () => {
    it('/towerdata/:towernam/:userid', () => {

    })

    it('/towerlist', () => {

    })

    it('/towertitle/:userid', () => {

    })

    it('/towertitleapply/:userid/:title', () => {
        
    })
})