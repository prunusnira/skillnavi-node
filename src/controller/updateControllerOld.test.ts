import request from 'supertest'
import app from '../index'

describe('UpdateController (OLD)', () => {
    it('/$/updateOld', () => {

    })

    it('/$/updateProfileOld/:gver', () => {

    })

    it('/$/updateSkillOld/:gver', () => {

    })
})